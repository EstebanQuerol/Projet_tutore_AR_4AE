Building ARtoolkit on ubuntu 11.10 32 bits.

Installed libraries:
	-freeglut3-dev 2.6.0-1ubuntu2
	-freeglut3 2.6.0-1ubuntu2
	-libgstreamer*
	-xorg dev
	-libjpeg62-dev libglib2.0-dev libgtk2.0-dev
	-gstreamer0.10-plugins-good-dbg 0.10.30-1ubuntu7.1


Libraries header's patch: 

include/AR/sys/videoLinuxV4L.h
lib/SRC/VideoLinuxV4L/video.c

for both we musy change the following includes:linux/videodev.h->libv4l1-videodev.h

V4L still not working

added the following lines to Configure file in video4linux and GStreamer  codes
	LIBS="-lpthread -lglut -lGLU -lGL -lXi -lX11 -lm $GST_LIBS"
        #LIBS="-lpthread -lglut -lGLU -lGL -lXi -lX11 -lm"
Solved the compiling problem for Gstreamer

Befose using GStreamer we must export the environement variable:

$export ARTOOLKIT_CONFIG="v4l2src device=/dev/video0 use-fixed-fps=false ! ffmpegcolorspace ! identity name=artoolkit ! fakesink"
